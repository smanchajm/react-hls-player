version: '3.8'

services:
  web:
    build: .
    container_name: react-hls-player
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "description=React HLS Player - Interface étudiants"
